mysql-connector-python>=8.2.0
Pillow>=10.1.0
reportlab>=4.0.8

# Note: tkinter is part of the Python standard library — no pip install needed.
# On Debian/Ubuntu install it with: sudo apt install python3-tk
